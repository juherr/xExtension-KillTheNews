<?php
declare(strict_types=1);
/** @var KillTheNewsExtension $this */
?>
<form action="<?= _url('extension', 'configure', 'e', urlencode($this->getName())); ?>" method="post">
	<input type="hidden" name="_csrf" value="<?= FreshRSS_Auth::csrfToken() ?>" />

	<?php if ($this->testResult !== null) { ?>
	<p class="<?= $this->testResult['ok'] ? 'alert alert-success' : 'alert alert-error' ?>">
		<?= htmlspecialchars($this->testResult['message'], ENT_QUOTES, 'UTF-8') ?>
	</p>
	<?php } ?>

	<div class="form-group">
		<label class="group-name" for="ktn_instance_url"><?= _t('ext.kill_the_news.instance_url') ?></label>
		<div class="group-controls">
			<input type="url" name="ktn_instance_url" id="ktn_instance_url" class="w100"
				value="<?= htmlspecialchars($this->instanceUrl, ENT_QUOTES, 'UTF-8') ?>"
				placeholder="https://news.example.com" />
			<p class="help"><?= _t('ext.kill_the_news.instance_url_help') ?></p>
		</div>
	</div>

	<div class="form-group">
		<label class="group-name" for="ktn_api_token"><?= _t('ext.kill_the_news.api_token') ?></label>
		<div class="group-controls">
			<input type="password" name="ktn_api_token" id="ktn_api_token" class="w100" autocomplete="off"
				placeholder="<?= $this->hasApiToken ? '••••••••' : '' ?>" />
			<p class="help"><?= _t('ext.kill_the_news.api_token_help') ?></p>
		</div>
	</div>

	<div class="form-group">
		<label class="group-name" for="ktn_verify_tls"><?= _t('ext.kill_the_news.verify_tls') ?></label>
		<div class="group-controls">
			<input type="checkbox" name="ktn_verify_tls" id="ktn_verify_tls" value="1" <?= $this->verifyTls ? 'checked' : '' ?> />
			<p class="help"><?= _t('ext.kill_the_news.verify_tls_help') ?></p>
		</div>
	</div>

	<div class="form-group form-actions">
		<div class="group-controls">
			<button type="submit" class="btn btn-important"><?= _t('gen.action.submit') ?></button>
			<button type="submit" name="ktn_test" value="1" class="btn"><?= _t('ext.kill_the_news.test_button') ?></button>
		</div>
	</div>
</form>
